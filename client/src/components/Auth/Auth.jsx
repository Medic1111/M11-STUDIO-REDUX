import FormWrapper from "../../common/FormWrapper/FormWrapper";
import AuthForm from "../AuthForm/AuthForm";

const Auth = () => {
  return (
    <FormWrapper>
      <AuthForm />
    </FormWrapper>
  );
};

export default Auth;
